# 🤝 초보자를 위한 깃허브 협업 가이드 (Github_manual.md)

이 문서는 Git이나 GitHub을 처음 사용해 보는 팀원분들도 쉽게 따라 할 수 있도록 만든 **초기 셋업 및 일상 개발 프로세스 가이드**입니다. 

우리 팀의 코드가 뒤섞이거나 유실되지 않도록, 개발을 시작할 때 이 문서를 꼭 옆에 띄워두고 한 단계씩 따라 해 주세요!

---

## 💡 쉽게 이해하는 브랜치(Branch) 개념

* **`main` (가게 진열대)**: 완성되어 완벽히 작동하는 코드만 올라가는 곳입니다. (배포용)
* **`dev` (주방의 공용 조리대)**: 팀원들의 요리(코드)가 한데 모여 완성본을 만드는 **기본 공간**입니다. 
* **`feat/이름` (개인 도마)**: 공용 공간을 어지럽히지 않고, 각자 자기 도마 위에서 요리(개발)를 진행합니다.

---

## 🛠️ 1. 최초 1회 셋업 (팀원 최초 설정)

원격 저장소(`https://github.com/SungWookJang8440/Workload-Profiling-and-Scheduling.git`)를 내 컴퓨터로 가져옵니다.

1. **원하는 폴더에서 터미널(PowerShell 또는 Git Bash)을 엽니다.**
2. **저장소를 복제(Clone)합니다:**
   ```powershell
   git clone https://github.com/SungWookJang8440/Workload-Profiling-and-Scheduling.git
   ```
3. **폴더 안으로 들어갑니다:**
   ```powershell
   cd Workload-Profiling-and-Scheduling
   ```
4. **기본 브랜치인 `dev` 브랜치가 로컬에 있는지 확인하고 전환합니다:**
   ```powershell
   git checkout dev
   ```

---

## 🔄 2. 일상적인 기능 개발 6단계 (매번 개발할 때마다!)

새로운 기능(예: 회원가입, 화면 UI 작업 등)을 추가할 때마다 **아래 6단계를 그대로 따라 해 주세요.**

### 1단계: 깃허브 이슈(Issue) 만들기
1. GitHub 저장소 홈페이지에서 **[Issues]** 탭 ➡️ **[New issue]** 단추를 클릭합니다.
2. 내가 오늘 할 일을 간단히 작성하고 우측의 **Assignees**에서 본인을 지정합니다.
3. 생성 완료 후 번호(예: **#7**)를 확인합니다.

---

### 2단계: 로컬 `dev` 최신화 (가장 중요! 🌟)
다른 팀원이 올린 최신 코드를 내 컴퓨터로 가져옵니다. **이 단계를 건너뛰면 나중에 충돌이 납니다!**
```powershell
# 1. 공용 조리대(dev)로 이동
git checkout dev

# 2. 깃허브의 최신 내용 가져오기
git pull origin dev
```

---

### 3단계: 내 개인 도마(브랜치) 만들기
최신화된 `dev`에서 나만의 작업 브랜치를 분기합니다.
* **이름 규칙**: `feat/이슈번호-간단한작업명`
```powershell
# 예시: 이슈번호 7번, 회원가입 기능 작업 시
git switch -c feat/7-signup
```
> [!NOTE]
> `git switch -c`는 새로운 브랜치를 만들면서 동시에 그 브랜치로 이동(전환)하라는 명령어입니다.

---

### 4단계: 신나게 코드 짜기 & 저장(Commit)
VS Code 등 에디터에서 코드를 짜고 저장합니다. 작업이 완료되면 Git에 기록을 남깁니다.
```powershell
# 1. 내가 변경한 모든 파일을 기록 후보에 올리기
git add .

# 2. 의미 있는 커밋 메시지와 함께 저장하기 (이름 규칙 준수!)
# 규칙: 접두사 : 작업내용 (콜론 앞뒤에 띄어쓰기를 꼭 넣어주세요!)
git commit -m "feat : 회원가입 API 추가 및 유효성 검사 구현"
```

---

### 5단계: 내 브랜치 깃허브에 올리기 (Push)
내가 만든 개인 도마(브랜치)를 깃허브 원격 저장소에 업로드합니다.
```powershell
git push -u origin feat/7-signup
```
> [!TIP]
> 처음 푸시할 때만 뒤에 `-u origin 브랜치명`을 붙이고, 그 다음 같은 브랜치에서 추가 커밋을 푸시할 때는 그냥 `git push`만 입력해도 됩니다.

---

### 6단계: Pull Request(PR) 생성하여 dev에 병합하기
1. 깃허브 홈페이지에 접속하면 초록색 **[Compare & pull request]** 버튼이 나타납니다. 클릭하세요!
2. **대상 브랜치 설정 (가장 중요) 🚨**:
   * **base: `dev`**  ⬅️  **compare: `feat/7-signup`**
   * (기본 설정이 `base: main`으로 되어 있을 수 있으니 반드시 **`dev`**로 변경해 주세요!)
3. **본문 작성**:
   * 내용에 `Resolves #7` (내 이슈번호)을 적어주면 병합 시 이슈가 자동으로 닫힙니다.
4. **리뷰 요청**: 팀원들에게 코드 리뷰를 요청하고, **최소 1명 이상 승인(Approve)**을 받으면 **[Merge pull request]**를 눌러 최종 병합합니다!

---

## 🚨 자주 발생하는 비상 상황 대처법 (FAQ)

### Q1. 코드 pull이나 push를 하려는데 거절당하고 이상한 에러가 나요!
* **이유**: 높은 확률로 내 로컬 컴퓨터의 코드 버전과 깃허브 저장소의 코드 버전이 맞지 않아서 그렇습니다.
* **해결법**: 
  1. 현재 작업하던 변경사항을 모두 커밋(`git commit`)해 둡니다.
  2. 내가 작업 중인 브랜치에서 아래 명령어로 깃허브의 `dev` 브랜치를 강제로 내 브랜치에 합쳐줍니다.
     ```powershell
     git pull origin dev
     ```
  3. 만약 파일 안의 코드가 겹쳐서 **Conflict(충돌)**이 발생하면 에디터에 빨갛게 표시됩니다. 팀원 혹은 충돌이 난 파트의 담당자와 상의하여 어떤 코드를 남길지 지운 후, 다시 저장하여 푸시하세요!

### Q2. 브랜치를 잘못 만들어서 main 브랜치에 커밋을 해버렸어요!
* **해결법**: 당황하지 마시고 터미널에 아래 명령어를 한 줄씩 차례대로 입력하세요.
  ```powershell
  # 1. 작업을 임시 저장하고 main 브랜치를 깃허브 상태로 강제 복구
  git reset --hard origin/main
  # 2. 다시 dev 브랜치로 돌아가기
  git checkout dev
  # 3. 최신화 후 브랜치 새로 만들기부터 차근차근 다시 시작!
  ```

---

**문의 및 가이드 수정 건의:** 👑 장성욱 (팀장)
